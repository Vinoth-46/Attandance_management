{
  "name": "attendance-management-system",
  "version": "1.0.0",
  "description": "MERN Attendance Management System with Face Recognition",
  "scripts": {
    "start": "npm start --prefix server",
    "build": "npm install --prefix server && cd client && npm install --include=dev && npm run build",
    "dev": "npm run dev --prefix server",
    "seed": "node server/seedDatabase.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "attendance",
    "mern",
    "face-recognition"
  ],
  "author": "Vinoth",
  "license": "MIT"
}