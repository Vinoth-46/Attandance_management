# MongoDB Connection
# Option 1: Use MongoDB Atlas (Cloud)
# MONGO_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/attendance_system?retryWrites=true&w=majority

# Option 2: Use Local MongoDB (Recommended for development)
MONGO_URI=mongodb://localhost:27017/attendance_system

# JWT Secret (REQUIRED in production - use a strong random string, min 32 characters)
# Generate one with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_jwt_secret_key_here_change_in_production

# Server Port
PORT=5000

# Node Environment (development or production)
NODE_ENV=development

# CORS: Allowed Origins (comma-separated, for production)
# Example: ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com
# Leave empty or set to * for development
ALLOWED_ORIGINS=*
